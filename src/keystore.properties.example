# Copy to src/keystore.properties (gitignored) for a real release key.
# TeamCity can set the same values as env: LIVE_STORE_FILE, LIVE_STORE_PASSWORD,
# LIVE_KEY_ALIAS, LIVE_KEY_PASSWORD.
storeFile=release.jks
storePassword=
keyAlias=fotriclive
keyPassword=
